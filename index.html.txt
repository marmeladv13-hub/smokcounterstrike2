<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
    <title>Раскидки Counter-Strike 2</title>
    <!-- Google Fonts & Font Awesome for icons -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,400;14..32,500;14..32,600;14..32,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: radial-gradient(circle at 10% 20%, #0a0f1f, #03060c);
            min-height: 100vh;
            color: #eef2ff;
            padding: 24px 20px 48px;
        }

        .app-container {
            max-width: 1300px;
            margin: 0 auto;
        }

        /* Header */
        .app-header {
            text-align: center;
            margin-bottom: 40px;
        }
        .app-header h1 {
            font-size: 2.5rem;
            font-weight: 800;
            background: linear-gradient(135deg, #ffd966, #ff9f4a);
            background-clip: text;
            -webkit-background-clip: text;
            color: transparent;
            text-shadow: 0 2px 5px rgba(0,0,0,0.2);
            letter-spacing: -0.5px;
        }
        .app-header h1 i {
            background: none;
            color: #ffb347;
            font-size: 2.2rem;
            margin-right: 10px;
            background-clip: unset;
            -webkit-background-clip: unset;
            color: #ffae42;
        }
        .sub {
            color: #8e9aaf;
            margin-top: 8px;
            font-weight: 500;
            font-size: 1rem;
        }

        /* Карты (главное меню) */
        .maps-grid {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 28px;
            margin-bottom: 50px;
        }
        .map-card {
            background: rgba(20, 28, 40, 0.8);
            backdrop-filter: blur(8px);
            border-radius: 32px;
            padding: 20px 20px 24px;
            width: 220px;
            text-align: center;
            cursor: pointer;
            transition: transform 0.25s ease, box-shadow 0.25s ease;
            border: 1px solid rgba(255, 180, 70, 0.3);
            box-shadow: 0 12px 28px rgba(0, 0, 0, 0.4);
        }
        .map-card:hover {
            transform: translateY(-8px);
            border-color: #ffaa33;
            box-shadow: 0 20px 30px -8px rgba(0,0,0,0.6);
            background: rgba(28, 38, 55, 0.9);
        }
        .map-icon {
            font-size: 4.2rem;
            background: #1e2a3a;
            width: 90px;
            height: 90px;
            line-height: 90px;
            text-align: center;
            margin: 0 auto 16px;
            border-radius: 30px;
            background: linear-gradient(145deg, #2a374b, #141c2c);
            box-shadow: inset 0 1px 1px rgba(255,255,255,0.1), 0 8px 14px rgba(0,0,0,0.3);
        }
        .map-card h3 {
            font-size: 1.7rem;
            font-weight: 700;
            letter-spacing: -0.3px;
            margin-bottom: 6px;
        }
        .map-card p {
            font-size: 0.85rem;
            color: #b9c7d9;
        }

        /* Область контента (список раскидок) */
        .smokes-section {
            background: rgba(12, 18, 28, 0.7);
            backdrop-filter: blur(4px);
            border-radius: 42px;
            padding: 28px 24px 36px;
            margin-top: 20px;
            border: 1px solid rgba(255, 160, 70, 0.3);
            transition: all 0.2s;
        }
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            flex-wrap: wrap;
            margin-bottom: 28px;
            border-bottom: 2px dashed #ffb34740;
            padding-bottom: 12px;
        }


moke-details {
            text-align: center;
            margin-top: 20px;
            font-size: 0.95rem;
            background: #00000040;
            border-radius: 32px;
            padding: 12px;
        }
        .footer-note {
            font-size: 0.7rem;
            text-align: center;
            margin-top: 40px;
            color: #6b7c93;
        }
        max-width: 680px {
            .maps-grid { gap: 16px; }
            .map-card { width: 160px; padding: 12px; }
            .map-icon { width: 65px; height: 65px; line-height: 65px; font-size: 3rem; }
            .map-card h3 { font-size: 1.3rem; }
            .smoke-card { width: 165px; }
            .section-header h2 { font-size: 1.4rem; }
        }
        button {
            background: none;
            font-family: inherit;
        }
        i, .fas, .far {
            pointer-events: none;
        }
    </style>
</head>
<body>
<div class="app-container">
    <div class="app-header">
        <h1><i class="fas fa-bomb"></i> Раскидки Counter-Strike 2</h1>
        <div class="sub">Смоки, позиции, прицелы — становись мастером тактики</div>
    </div>

    <!-- Главное меню: карты -->
    <div class="maps-grid" id="mapsGrid">
        <!-- Dust2 -->
        <div class="map-card" data-map="dust2">
            <div class="map-icon"><i class="fas fa-mountain"></i></div>
            <h3>Dust II</h3>
            <p>Классика & раскидки</p>
        </div>
        <!-- Mirage -->
        <div class="map-card" data-map="mirage">
            <div class="map-icon"><i class="fas fa-palmtree"></i></div>
            <h3>Mirage</h3>
            <p>Окна, аппартаменты</p>
        </div>
        <!-- Inferno (экшен) -->
        <div class="map-card" data-map="inferno">
            <div class="map-icon"><i class="fas fa-fire"></i></div>
            <h3>Inferno</h3>
            <p>Банана, мид</p>
        </div>
    </div>

    <!-- Динамическая область: список раскидок выбранной карты -->
    <div id="smokesPanel" class="smokes-section" style="display: none;">
        <div class="section-header">
            <h2 id="currentMapTitle"><i class="fas fa-smog"></i> Раскидки</h2>
            <button class="back-btn" id="backToMapsBtn"><i class="fas fa-arrow-left"></i> Выбрать другую карту</button>
        </div>
        <div id="smokesGridContainer" class="smokes-grid">
            <!-- сюда динамически карточки смоков -->
        </div>
    </div>
    <div class="footer-note">
        <i class="fas fa-mouse-pointer"></i> Нажми на карту ? выбери раскидку ? детальная схема: позиция, прицел, точка дыма
    </div>
</div>

<!-- Модальное окно с canvas схемой -->
<div id="smokeModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <h3 id="modalTitle">Smoke</h3>
            <button class="close-modal" id="closeModalBtn">&times;</button>
        </div>
        <div class="canvas-container">
            <canvas id="smokeCanvas" width="650" height="380" style="width:100%; height:auto; max-width:650px; aspect-ratio:650/380"></canvas>
        </div>
        <div class="smoke-details" id="modalDetails">
            <!-- текстовая дополнительная подсказка -->
        </div>
    </div>
</div>

<script>
    // ---------- БАЗА ДАННЫХ РАСКИДОК (SMOKES) ДЛЯ ТРЁХ КАРТ ----------
    const MAPS_DATA = {
        dust2: {
            name: "Dust II",
            icon: "fa-mountain",
            smokes: [
                { id: "d2_long", title: "Long Doors Smoke", 
                  positionDesc: "Встать у правого угла на T базе, упереться в стену", 
                  aimDesc: "Целиться в верхнюю часть деревянных дверей (в перекрестие на текстуре)", 
                  landDesc: "Дым падает в проход Long A, закрывает обзор из защиты" },
                { id: "d2_mid", title: "Mid Doors Smoke", 
                  positionDesc: "B туннель, стоять у левой стены около ящиков", 
                  aimDesc: "Прицел в дыру над Mid дверьми (выше арки)", 
                  landDesc: "Дым закрывает средние ворота, перекрывает снайперскую линию" },


.section-header h2 {
            font-size: 1.8rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .back-btn {
            background: none;
            border: 1px solid #ffb347;
            color: #ffcf8a;
            padding: 8px 20px;
            border-radius: 60px;
            font-weight: 600;
            cursor: pointer;
            transition: 0.2s;
            font-size: 0.9rem;
        }
        .back-btn:hover {
            background: #ffb34722;
            color: #ffdd99;
            transform: scale(0.97);
        }
        .smokes-grid {
            display: flex;
            flex-wrap: wrap;
            gap: 25px;
            justify-content: center;
        }
        .smoke-card {
            background: #11171fdd;
            border-radius: 28px;
            width: 210px;
            padding: 18px 12px 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s;
            border: 1px solid #2d3e54;
            backdrop-filter: blur(4px);
        }
        .smoke-card:hover {
            background: #1e2a34;
            border-color: #ffaa55;
            transform: scale(1.02);
            box-shadow: 0 12px 18px rgba(0,0,0,0.3);
        }
        .smoke-img-placeholder {
            font-size: 3.4rem;
            background: #0b111a;
            border-radius: 40px;
            width: 70px;
            height: 70px;
            line-height: 70px;
            margin: 0 auto 14px;
            box-shadow: inset 0 1px 2px #ffffff20, 0 5px 10px #00000040;
        }
        .smoke-card h4 {
            font-size: 1.2rem;
            font-weight: 600;
            margin-bottom: 8px;
        }
        .smoke-card p {
            font-size: 0.75rem;
            color: #b9c2d0;
        }
        .badge-smoke {
            background: #ff9f4a20;
            display: inline-block;
            padding: 3px 12px;
            border-radius: 50px;
            font-size: 0.7rem;
            margin-top: 10px;
            color: #ffbc6e;
        }

        /* Модальное окно */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.85);
            backdrop-filter: blur(8px);
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }
        .modal-content {
            background: #0f172ad9;
            border-radius: 48px;
            max-width: 750px;
            width: 90%;
            padding: 20px 24px 30px;
            border: 1px solid #ffb264;
            box-shadow: 0 25px 40px rgba(0,0,0,0.6);
            animation: fadeUp 0.25s ease;
        }
        @keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            border-bottom: 1px solid #ffb34760;
            padding-bottom: 12px;
        }
        .modal-header h3 {
            font-size: 1.7rem;
            font-weight: 700;
        }
        .close-modal {
            background: none;
            border: none;
            font-size: 28px;
            cursor: pointer;
            color: #ffbc76;
            transition: 0.1s;
        }
        .close-modal:hover {
            color: #fff;
            transform: scale(1.1);
        }
        .canvas-container {
            display: flex;
            justify-content: center;
            margin: 20px 0 10px;
        }
        canvas {
            background: #1e2a32;
            border-radius: 28px;
            box-shadow: 0 12px 18px black;
            width: 100%;
            height: auto;
            border: 1px solid #ffb26660;
        }
        .s


{ id: "d2_b", title: "B Site Smoke (Plat)", 
                  positionDesc: "T спавн, у бетонного блока слева", 
                  aimDesc: "Край окна на верхней платформе B", 
                  landDesc: "Дым ложится на B site возле платформы и ящиков" }
            ]
        },
        mirage: {
            name: "Mirage",
            icon: "fa-palmtree",
            smokes: [
                { id: "mrg_window", title: "Window Smoke (Mid)", 
                  positionDesc: "T аппартаменты, прижаться к подоконнику слева", 
                  aimDesc: "Верхний правый угол рамы окна (белая точка)", 
                  landDesc: "Дым точно в окно мида, отсекает AWP" },
                { id: "mrg_jungle", title: "Jungle Connector Smoke", 
                  positionDesc: "T спавн, за коробкой у стены", 
                  aimDesc: "Между пальмой и лампой в небе", 
                  landDesc: "Дым падает в джунгли, блокирует коннектор к A" },
                { id: "mrg_bshort", title: "B Short Smoke", 
                  positionDesc: "B аппартаменты, у скамейки", 
                  aimDesc: "В угол стены на выступе", 
                  landDesc: "Дым накрывает вход на B site со стороны шорта" }
            ]
        },
        inferno: {
            name: "Inferno",
            icon: "fa-fire",
            smokes: [
                { id: "inf_mid", title: "Mid Smoke (Top Mid)", 
                  positionDesc: "T спавн, возле фонтана, смотреть в арку", 
                  aimDesc: "В самую верхнюю точку арки (трещина)", 
                  landDesc: "Дым закрывает вид со стороны мида и второго мида" },
                { id: "inf_banana", title: "Banana Deep Smoke", 
                  positionDesc: "T аппартаменты, у бочки с правой стороны", 
                  aimDesc: "В вывеску ‘Truck’ над входом", 
                  landDesc: "Дым ложится перед входом на Banana, мешает заходу CT" },
                { id: "inf_asite", title: "A Site Pit Smoke", 
                  positionDesc: "Mid, у ящиков возле лестницы", 
                  aimDesc: "В угол дома (красная черепица)", 
                  landDesc: "Дым падает в яму на A site, закрывает обзор с питса" }
            ]
        }
    };

    // Текущее состояние
    let currentMapId = null;   // 'dust2', 'mirage', 'inferno'
    let currentSmoke = null;   // объект выбранного смока

    // DOM элементы
    const mapsGrid = document.getElementById('mapsGrid');
    const smokesPanel = document.getElementById('smokesPanel');
    const smokesGridContainer = document.getElementById('smokesGridContainer');
    const currentMapTitleSpan = document.getElementById('currentMapTitle');
    const backToMapsBtn = document.getElementById('backToMapsBtn');
    const modal = document.getElementById('smokeModal');
    const modalTitle = document.getElementById('modalTitle');
    const smokeCanvas = document.getElementById('smokeCanvas');
    const modalDetails = document.getElementById('modalDetails');
    const closeModalBtn = document.getElementById('closeModalBtn');

    // ---- Функция отрисовки canvas схемы (картинка: встать, целиться, результат) ----
    function drawSmokeDiagram(canvas, smokeData) {
        const ctx = canvas.getContext('2d');
        const w = canvas.width;
        const h = canvas.height;
        
        // Фон с текстурой
        ctx.fillStyle = "#121c26";
        ctx.fillRect(0, 0, w, h);
        // Сетка-декор
        ctx.strokeStyle = "#2e445e";
        ctx.lineWidth = 1;
        for (let i = 0; i < w; i += 40) {
            ctx.beginPath();
            ctx.moveTo(i, 0);
            ctx.lineTo(i, h);
            ctx.stroke();
            ctx.beginPath();
            ctx.moveTo(0, i % h);
            ctx.lineTo(w, i % h);
            ctx.stroke();
        }
        
        // Заголовок сверху
        ctx.font = "bold 16px 'Inter', sans-serif";
        ctx.fillStyle = "#ffdd99";
        ctx.shadowBlur = 0;
        ctx.fillText(smokeData.title, w/2 - ctx.measureText(smokeData.title).width/2, 32);
        
        // Рисуем 3 зоны: (1) позиция игрока, (2) прицел, (